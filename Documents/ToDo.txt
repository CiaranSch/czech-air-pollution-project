ToDo
- Add labels to EU policy guidelines
- Specify a minimum size for map chart (prevent being cut off)
- Rmarkdown download to csv/word file

Done
- Remove options from X-Axis based on aggregation picked
- If averaged weekly graph should be days of week, otherwise hours
- Dynamic plot title
- Dynamic x/y axis labels
- Change xaxis selection to a subset rather than entirely different vectors
- Do we need a yearly average aggregate? PM2.5, PM10, N02
- Auto add EU policy lines if correct chart
- Add table below google maps chart


Data (basic table) tableOutput renderTable
Data (fancy table) dataTableOutput renderDataTable